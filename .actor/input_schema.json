{
    "$schema": "https://apify.com/schemas/v1/input.ide.json",
    "title": "Datart.cz Product Scraper",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "One or more datart.cz URLs to start from. Use the homepage to crawl everything or a category page (e.g. https://www.datart.cz/zamky-na-kolo.html) to scope the run to one category.",
            "editor": "requestListSources",
            "prefill": [
                {
                    "url": "https://www.datart.cz/zamky-na-kolo.html"
                }
            ]
        },
        "maxRequestsPerCrawl": {
            "title": "Max Requests per Crawl",
            "type": "integer",
            "description": "Maximum number of pages (categories + products) the crawler may fetch. Set to 0 for unlimited.",
            "default": 200,
            "minimum": 0
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Proxy settings for avoiding IP blocks. Enable Apify Proxy for large-scale runs.",
            "editor": "proxy",
            "default": {
                "useApifyProxy": true,
                "apifyProxyGroups": ["RESIDENTIAL"],
                "apifyProxyCountry": "CZ"
            }
        }
    },
    "required": []
}
